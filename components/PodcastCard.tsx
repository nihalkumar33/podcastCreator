import React from 'react'

const PodcastCard = ({
    imgUrl, description, podcastId, title
}: {
    imgUrl: string
    description: string
    podcastId: number
    title: string
}) => {
  return (
    <div>PodcastCard</div>
  )
}

export default PodcastCard